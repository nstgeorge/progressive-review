'use strict';

/**
 * about-characteristic controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::about-characteristic.about-characteristic');
