'use strict';

/**
 * about-characteristic router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::about-characteristic.about-characteristic');
