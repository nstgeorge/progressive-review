'use strict';

/**
 * about-characteristic service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::about-characteristic.about-characteristic');
